#include <stdio.h>
int main() {
    int myNum = 10;
    int _count = 20;
    printf("myNum: %d, _count: %d\n", myNum, _count);
    return 0;
}
//tell that not every name can be used to name a variable like int